<Window
    x:Class="Tavstal.MesterMC.Launcher.Views.LauncherWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Tavstal.MesterMC.Launcher.Models"
    xmlns:vm="clr-namespace:Tavstal.MesterMC.Launcher.Views.Models"
    Title="MMC Launcher"
    Width="1280"
    Height="665"
    d:DesignHeight="665"
    d:DesignWidth="1280"
    x:DataType="vm:LauncherViewModel"
    Background="Transparent"
    CanResize="False"
    ExtendClientAreaToDecorationsHint="False"
    Icon="avares://MMC-Launcher/Assets/icons/favicon.png"
    ShowInTaskbar="True"
    SystemDecorations="None"
    TransparencyLevelHint="Transparent"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <models:LoginStatusConverter x:Key="LoginConverter" />
    </Window.Resources>

    <Viewbox x:Name="RootViewbox" Stretch="Uniform">
        <Grid Width="1280" Height="665">
            <!--  Background Image  -->
            <Image
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Source="avares://MMC-Launcher/Assets/screens/mmc-launcher-bg.png"
                Stretch="Fill"
                PointerPressed="DragStart_PointerPressed"/>

            <!--  Logo  -->
            <Image
                Height="120"
                Margin="0,0,30,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Source="avares://MMC-Launcher/Assets/logos/mmc-logo.png"
                PointerPressed="DragStart_PointerPressed"/>

            <!--  Character Image  -->
            <Image
                Height="250"
                Margin="0,0,105,20"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Opacity="1"
                Source="avares://MMC-Launcher/Assets/characters/aron.png" 
                PointerPressed="DragStart_PointerPressed"/>

            <!--#region Left Side Panel-->
            <Grid
                Width="400"
                Height="574"
                Margin="0,-4.5,0,0"
                HorizontalAlignment="Left"
                Background="Transparent"
                RowDefinitions="*, *, *, *"
                RowSpacing="5"
                PointerPressed="DragStart_PointerPressed">
                <!--  Facebook Button  -->
                <HyperlinkButton
                    Grid.Row="0"
                    Classes="FacebookButton"
                    NavigateUri="https://www.facebook.com/mestermc.hu">
                    <Image />
                </HyperlinkButton>

                <!--  Server List & Help Button  -->
                <Grid
                    Grid.Row="1"
                    ColumnDefinitions="*, *"
                    ColumnSpacing="2">
                    <!--  ServerList Button  -->
                    <HyperlinkButton
                        Grid.Column="0"
                        Classes="ServerListButton"
                        NavigateUri="https://mestermc.hu/szerverek">
                        <Image />
                    </HyperlinkButton>

                    <!--  Help Button  -->
                    <HyperlinkButton
                        Grid.Column="1"
                        Classes="HelpButton"
                        NavigateUri="mailto:info@mestermc.hu">
                        <Image />
                    </HyperlinkButton>
                </Grid>

                <!--  Banyakozpont Button  -->
                <HyperlinkButton
                    Grid.Row="2"
                    Classes="BanyakozpontButton"
                    NavigateUri="https://banyakozpont.mestermc.hu">
                    <Image />
                </HyperlinkButton>

                <!--  Banyaszerme Button  -->
                <HyperlinkButton
                    Grid.Row="3"
                    Classes="BanyaszermeButton"
                    NavigateUri="https://mestermc.hu/banyaszerme">
                    <Image />
                </HyperlinkButton>
            </Grid>
            <!--#endregion-->

            <!--#region News Panel-->
            <Grid
                Height="571"
                Margin="405,-4.5,315,0"
                PointerPressed="DragStart_PointerPressed">
                <!--  News Title  -->
                <StackPanel Margin="25,40,0,0" Orientation="Horizontal">
                    <TextBlock
                        FontSize="36"
                        FontWeight="300"
                        Foreground="#ffff00"
                        Text="FRISS" />
                    <TextBlock
                        FontSize="36"
                        FontWeight="600"
                        Foreground="White"
                        Text=" HÍREINK" />
                </StackPanel>

                <!--  Pages  -->
                <Grid
                    Margin="0,-15,25,0"
                    HorizontalAlignment="Right"
                    ColumnDefinitions="Auto, Auto"
                    ColumnSpacing="10"
                    RowDefinitions="Auto, Auto"
                    RowSpacing="5"
                    PointerPressed="DragStart_PointerPressed">
                    <!--  PrevPage Button  -->
                    <Button
                        Grid.Row="0"
                        Grid.Column="0"
                        Height="35"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        Classes="PrevPageButton"
                        Command="{Binding PreviousNewsCommand}">
                        <Image />
                    </Button>
                    <!--  NextPage Button  -->
                    <Button
                        Grid.Row="0"
                        Grid.Column="1"
                        Height="35"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        Classes="NextPageButton"
                        Command="{Binding NextNewsCommand}">
                        <Image />
                    </Button>
                    <!--  Page Text  -->
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        HorizontalAlignment="Center"
                        FontWeight="600"
                        Foreground="#727170"
                        Text="{Binding NewsPageDisplay}" />
                </Grid>

                <!--  News Content  -->
                <Grid
                    Height="435"
                    Margin="25,115,25,25"
                    VerticalAlignment="Top"
                    RowDefinitions="Auto, Auto, *"
                    RowSpacing="8"
                    PointerPressed="DragStart_PointerPressed">
                    <!--  Content Title  -->
                    <TextBlock
                        FontWeight="600"
                        Foreground="#ffb400"
                        Text="{Binding SelectedNewsItem.Title}" />

                    <!--  Content Banner  -->
                    <Image Grid.Row="1" Source="{Binding SelectedNewsItem.Banner}" />

                    <!--  Content Text  -->
                    <TextBlock
                        Grid.Row="2"
                        FontWeight="300"
                        Foreground="White"
                        Text="{Binding SelectedNewsItem.Content}"
                        TextWrapping="Wrap" />
                </Grid>

            </Grid>
            <!--#endregion-->

            <!--#region Login Panel-->
            <Grid
                Width="285"
                Height="350"
                Margin="0,-4.5,29,0"
                HorizontalAlignment="Right"
                Background="Transparent"
                IsVisible="{Binding !isLoggingIn}"
                RowDefinitions="Auto, Auto, Auto, Auto"
                RowSpacing="8">

                <!--  Username TextBox  -->
                <ComboBox
                    Grid.Row="0"
                    Width="230"
                    Height="30"
                    Classes="LoginBox"
                    IsEditable="True"
                    GotFocus="Username_GotFocus"
                    ItemsSource="{Binding SavedUsernames}"
                    SelectedItem="{Binding Username}"
                    Text="{Binding Username}"
                    PlaceholderText="Felhasználónév"
                    >
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Grid.Column="0" 
                                           Text="{Binding}" 
                                           VerticalAlignment="Center" />
                                <Button Grid.Column="1" 
                                        Content="×" 
                                        Command="{Binding $parent[ComboBox].((vm:LauncherViewModel)DataContext).RemoveUserCommand}"
                                        CommandParameter="{Binding}"
                                        Tag="{Binding}"
                                        Margin="5,0,0,0"
                                        Padding="5,0"
                                        Background="Transparent"
                                        FontSize="16"
                                        BorderThickness="0" />
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!--  Password TextBox  -->
                <TextBox
                    Grid.Row="1"
                    Width="230"
                    Height="30"
                    Classes="LoginBox"
                    IsEnabled="{Binding !OfflineMode}"
                    RevealPassword="False"
                    PasswordChar="●"
                    Text="{Binding Password}"
                    Watermark="Jelszó" />

                <!--  Offline Login CheckBox  -->
                <Border
                    Grid.Row="2"
                    Margin="30,0,30,0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    ToolTip.Placement="Left">
                     <ToolTip.Tip>
                        <StackPanel>
                            <TextBlock>Jelenleg csak az offline mód elérhető.</TextBlock>
                        </StackPanel>
                    </ToolTip.Tip>

                    <CheckBox
                        Padding="0"
                        Classes="OfflineCheckBox"
                        FontSize="18"
                        FontWeight="500"
                        Foreground="White"
                        IsChecked="{Binding OfflineMode, Mode=TwoWay}"
                        IsEnabled="False">
                          <Border Width="205" CornerRadius="4">
                            <TextBlock Text="Offline mód" />
                        </Border>
                    </CheckBox>
                </Border>

                <!--  Login Button  -->
                <Button
                    Grid.Row="3"
                    Height="72"
                    Margin="0,5,0,0"
                    Classes="LoginButton"
                    Command="{Binding LoginCommand}">
                    <Image />
                </Button>
            </Grid>
            <!--#endregion-->

            <!--#region TFA Panel-->
            <Grid
                Width="285"
                Height="350"
                Margin="0,-4.5,29,0"
                HorizontalAlignment="Right"
                Background="Transparent"
                IsVisible="{Binding isTFA}"
                RowDefinitions="Auto, Auto, Auto"
                RowSpacing="8">
                <!-- Code TextBox  -->
                <TextBox
                    Grid.Row="0"
                    Width="230"
                    Height="30"
                    Classes="LoginBox"
                    Text="{Binding TfaCode}"
                    Watermark="Kétfaktoros hitelesítő"
                    MaxLength="6"/>

                <!--  Login Button  -->
                <Button
                    Grid.Row="1"
                    Height="72"
                    Margin="0,5,0,0"
                    Classes="LoginButton"
                    Command="{Binding SubmitTFACommand}">
                    <Image />
                </Button>
                
                <!--  Back Button  -->
                <Button
                    Grid.Row="2"
                    Height="72"
                    Margin="26,-15,26,0"
                    Classes="ErrorBackButton"
                    Command="{Binding BackCommand}">
                    <Image />
                </Button>
            </Grid>
            <!--#endregion-->
            
            <!--#region Auth Feedback-->
            <Grid
                Width="285"
                Height="350"
                Margin="0,-4.5,29,0"
                HorizontalAlignment="Right"
                Background="Transparent"
                IsVisible="{Binding shouldShowFeedback}"
                RowDefinitions="Auto, Auto">

                <!--  Status Image  -->
                <Image
                    Grid.Row="0"
                    IsVisible="{Binding !isError}"
                    Source="{Binding LoginStatus, Converter={StaticResource LoginConverter}}" />

                <!--  Error Background Image  -->
                <Image
                    Grid.Row="0"
                    Margin="20,-10,20,0"
                    IsVisible="{Binding isError}"
                    Source="avares://MMC-Launcher/Assets/zone/zone-error.png" />

                <!--  Error TextBox  -->
                <TextBlock
                    Grid.Row="0"
                    Width="230"
                    MaxHeight="125"
                    Margin="0"
                    VerticalAlignment="Top"
                    Foreground="Red"
                    IsVisible="{Binding isError}"
                    Text="{Binding ErrorMessage}"
                    TextWrapping="Wrap" />

                <!--  Back Button  -->
                <Button
                    Grid.Row="1"
                    Height="72"
                    Margin="20,-15,20,0"
                    Classes="ErrorBackButton"
                    Command="{Binding BackCommand}"
                    IsVisible="{Binding isError}">
                    <Image />
                </Button>
            </Grid>
            <!--#endregion-->

            <!--  Close Button  -->
            <Button
                Height="35"
                Margin="0,30,10,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="Transparent"
                Classes="ExitButton"
                Command="{Binding CloseWindowCommand}">
                <Image />
            </Button>
        </Grid>
    </Viewbox>
</Window>
